import { Alegreya, Mulish } from "next/font/google";

export const alegrea = Alegreya({
  subsets: ["latin", "cyrillic"],
  display: "swap",
  variable: "--font-alegrea",
});
export const mulish = Mulish({
  subsets: ["latin", "cyrillic"],
  display: "swap",
  variable: "--font-mulish",
});
